# Required: Anthropic API key for Claude Code
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Required: OpenAI API key for Mem0 embeddings
OPENAI_API_KEY=sk-your-key-here

# Optional: absolute path to workspace directory
# If using `make up`, this defaults to ./workspace resolved to an absolute path.
# WORKSPACE=/absolute/path/to/workspace
